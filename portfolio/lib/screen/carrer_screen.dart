import 'package:flutter/material.dart';

class CarrerScreen extends StatelessWidget {
  const CarrerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
